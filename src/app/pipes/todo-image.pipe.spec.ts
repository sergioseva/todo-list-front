import { TodoImagePipe } from './todo-image.pipe';

describe('TodoImagePipe', () => {
  it('create an instance', () => {
    const pipe = new TodoImagePipe();
    expect(pipe).toBeTruthy();
  });
});
